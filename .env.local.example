# Convex 自动生成（运行 npx convex dev 后会写入）
CONVEX_DEPLOYMENT=dev:your-deployment-name
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Agent 上报用的共享密钥（自定义，与 log-activity 脚本里的要一致）
AGENT_LOG_TOKEN=replace-with-random-string